apiVersion: v1
kind: Service
metadata:
  name: ai_insurance-service
spec:
  type: LoadBalancer
  selector:
    app: ai_insurance
  ports:
    - name: ai_insurance-port
      protocol: TCP
      port: 80
      targetPort: 5000